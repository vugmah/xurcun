import CatalogPage from "./CatalogPage";

// Café menu = catalog categories/items with menuType "cafe" (in-store coffee shops).
export default function CafeMenuPage() {
  return <CatalogPage menuType="cafe" heading="Kafe Menyu" />;
}
